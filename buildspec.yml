version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - echo Build started on `date`
      - eb_codebuild_settings:
          module_name: my-portfolio
